services:
  - type: web
    name: odoo
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libsasl2-dev libldap2-dev libssl-dev
      pip install -r requirements.txt
    startCommand: ./odoo-bin -c ./odoo.conf
    
